# 🚀 Diretório: /applications

## Propósito

Este diretório contém as aplicações de alto nível (os programas "executáveis") que o usuário final irá rodar. Essas aplicações importam e utilizam a biblioteca principal definida no diretório `/src`.

## Arquivos Esperados

* **Fase 1 (Animação):** O subdiretório `/phase1_animation` contém os scripts Python (`.py`) para a cabeça animada (ex: usando Pygame).
* **Fase 2 (Robótica):** O subdiretório `/phase2_robot` contém os scripts Python (`.py`) para controlar os servomotores.
* **Arquivos de Configuração:** O subdiretório `/config` contém arquivos (`.json`, `.yaml`) que definem parâmetros como a geometria dos microfones, limiares de som e portas de GPIO.
* **Listas de Dependência:** `requirements.txt` específicos para cada aplicação, se necessário.
